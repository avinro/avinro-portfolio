---
title: Glyph Design System
slug: glyph-design-system
year: 2024
category: Visual system
summary: A token-first, multi-brand design system built to support light/dark modes and three product surfaces from a single shared foundation.
coverImage: /works/glyph-design-system/cover.svg
coverAspect: landscape
order: 2
featured: true
featuredOrder: 20
tools:
  - Figma
  - Storybook
  - Tailwind
tags:
  - Design system
  - Tokens
  - Multi-brand
gallery:
  - src: /works/glyph-design-system/gallery-1.svg
    alt: Token foundation color scales
    caption: "Color scales derived from perceptual lightness steps, not arbitrary hex picks. Both light and dark modes share the same semantic token names."
    aspect: landscape
  - src: /works/glyph-design-system/gallery-2.svg
    alt: Typography scale
    caption: "7-step type scale with display, heading, body, label, and mono variants. All step sizes are referenced by semantic tokens, not raw values."
    aspect: landscape
  - src: /works/glyph-design-system/gallery-3.svg
    alt: Component library form elements
    caption: "Form components in all 4 states — default, focused, error, disabled. Each state maps to a semantic token, not a hardcoded color."
    aspect: landscape
  - src: /works/glyph-design-system/gallery-4.svg
    alt: Dark mode component set
    caption: "Dark mode flipped entirely via token swap — zero per-component overrides required."
    aspect: landscape
  - src: /works/glyph-design-system/gallery-5.svg
    alt: Spacing and layout tokens
    caption: "Spacing tokens built on a 4px base grid with named aliases (xs to 3xl) for predictable rhythm across components."
    aspect: landscape
---

Glyph started from a recurring pain: every new product surface required re-solving the same visual problems — color contrast, spacing rhythm, and interactive states — from scratch.

The system is built token-first, meaning decisions about color, spacing, and type are encoded as named values before any component is drawn. This makes brand updates, dark mode, and multi-product theming a configuration problem rather than a redesign.
